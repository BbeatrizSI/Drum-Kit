"use strict";function removeTransition(e){"transform"===e.propertyName&&this.classList.remove("playing")}window.addEventListener("keydown",(function(e){const t=document.querySelector(`audio[data-key="${e.code}"]`),n=document.querySelector(`.key[data-key="${e.code}"]`);t&&(t.currentTime=0,t.play(),n.classList.add("playing"))}));const keys=document.querySelectorAll(".key");keys.forEach(e=>e.addEventListener("transitionend",removeTransition));